# Optional for production or preview builds when the frontend is not using the Vite dev proxy.
VITE_API_BASE_URL=https://localhost:7042/api

# Optional for local development if you change the API launch profile port.
VITE_PROXY_TARGET=https://localhost:7042
